import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.android.tuner',
  appName: 'tuner',
  webDir: 'dist/tuner',
  bundledWebRuntime: false
};

export default config;
